import { heroui } from "@heroui/react";

export default heroui({
  themes: {
    light: {
      colors: {
        background: "#fd9a00",
        foreground: "#fafafa",
        primary: {
          DEFAULT: "#e60076",
        },
      },
    },
  },
});
