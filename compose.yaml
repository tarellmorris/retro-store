services:
  frontend:
    build:
      context: .
      dockerfile: ./src/main/js/Dockerfile
    ports:
      - "3000:3000"
    networks:
      - front-tier
      - back-tier
networks:
  front-tier: { }
  back-tier: { }
